# Dev seeds for Escuela de Dinero.
#
# Every table this domain reads (people, conversations, messages, diagnosticos,
# movimientos, policing_events, outbound_messages, kubo_referrals) is owned and
# written by the Python/FastAPI bot — do NOT seed them from here. Ledgr owns
# only `users`, and users are created by hand rather than seeded.
