# Dev seeds for Aumenta Mi Pensión.
# The upstream AMP tables (agents, customers, conversations, consultations,
# pension_cases, etc.) are owned by the Python/FastAPI service — do NOT seed
# them from here. Only seed local Ledgr-owned tables (e.g. stripe_payments).
